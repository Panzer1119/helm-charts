apiVersion: v1
kind: Service
metadata:
  name: {{ include "servarr-template.fullname" . }}
  labels:
    {{- include "servarr-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servarr-template.selectorLabels" . | nindent 4 }}
