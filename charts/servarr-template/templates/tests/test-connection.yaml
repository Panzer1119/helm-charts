apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "servarr-template.fullname" . }}-test-connection"
  labels:
    {{- include "servarr-template.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "servarr-template.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
