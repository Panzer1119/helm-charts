apiVersion: v1
kind: Service
metadata:
  name: {{ include "servarr-single.fullname" . }}
  labels:
    {{- include "servarr-single.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "servarr-single.selectorLabels" . | nindent 4 }}
